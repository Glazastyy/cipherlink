export const invalidEncryptedPayloadResponse = new Response('Invalid encrypted payload', { status: 400 });
export const invalidRedirectTargetResponse = new Response('Invalid redirect target', { status: 400 });
export const serviceMisconfiguredResponse = new Response('Service misconfigured', { status: 500 });
